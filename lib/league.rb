class League
  def initialize
  end

  def count_of_teams(teams_data)
    teams_data.count
  end

  def best_offense(teams_data) #change argument as needed
    # team_id = averaging_hash.key(averaging_hash.values.max)
    # team_name_from_id(team_id)
  end
end
